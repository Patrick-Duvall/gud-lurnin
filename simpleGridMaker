function grid(size){
  
  var ans = ''
  for(let i = 0; i < size; i++){
    for (let j = 0; j < size; j++){
      if( i%2 ===1)(j % 2 ===0) ? ans += '#' : ans +=' ';
      else {(j % 2 ===0) ? ans += ' ' : ans +='#';}
    }
   ans +='\n'
  }
  console.log(ans)
}
